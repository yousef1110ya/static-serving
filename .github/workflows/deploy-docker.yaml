name: deploy docker
#
on:
    push:
        branches: [master]
    workflow_dispatch:

env:
    IMAGE_NAME: static-serving 

jobs:
    build_and_push:
        name: Build and Push
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v3

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@v2

            - name: Log in to DockerHub
              uses: docker/login-action@v2
              with:
                  username: ${{ secrets.DOCKERHUB_USERNAME }}
                  password: ${{ secrets.DOCKERHUB_PASSWORD }}

            - name: Get version info
              id: version
              run: |
                  VERSION="${{ github.ref_name }}"
                  VERSION1="${{ github.ref_name }}-${{ github.run_number }}"
                  echo "version=$VERSION" >> $GITHUB_OUTPUT
                  echo "version1=$VERSION2" >> $GITHUB_OUTPUT

            - name: Build and push Docker image
              run: |
                  docker build --pull . --file Dockerfile --tag ${IMAGE_NAME}
                  VERSION="${{ steps.version.outputs.version }}"
                  IMAGE="birobyte1110/${IMAGE_NAME}"

                  # Tag and push with branch name
                  docker tag "${IMAGE_NAME}" "${IMAGE}:${VERSION}"
                  echo "${IMAGE}:${VERSION}"
                  docker push "${IMAGE}:${VERSION}"
